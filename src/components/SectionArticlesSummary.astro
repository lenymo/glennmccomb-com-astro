---
import ArticleSummary from "./ArticleSummary.astro";
import Container from "./grid/Container.astro";
import Row from "./grid/Row.astro";
import type { CollectionEntry } from "astro:content";

interface Props {
  articles: CollectionEntry<"articles">[];
  isFirstSection?: boolean;
}

const { articles, isFirstSection = false } = Astro.props;

const sectionClasses = [
  "section",
  "section__articles-summary",
  isFirstSection && "-is-first-section",
]
  .filter(Boolean)
  .join(" ");
---

<section class={sectionClasses}>
  <Container classNameSuffix="articles-summary">
    <Row classNameSuffix="articles-summary">
      {articles.map((article) => <ArticleSummary article={article} />)}
    </Row>
  </Container>
</section>
